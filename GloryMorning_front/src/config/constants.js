const UI_VERSION_PATH = '--';  
const constants = {
    PATH: '//' + UI_VERSION_PATH + '/idle',
    CODE: {
    }
};

const PATH = constants;
const GNB_CODE = constants.CODE;


// [CUG] [Digicap] CUG_PROP 변수값
const CUG_PROP = constants.CUG_PROP;

export {
    UI_VERSION_PATH,
    PATH, GNB_CODE
};

export default constants;
